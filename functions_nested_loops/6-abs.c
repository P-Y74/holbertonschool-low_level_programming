#include <stdio.h>
#include "main.h"

/**
 * _abs - Compute the absolute value of an integer
 * @int: int to compare
 *
 * Return: 0
 */
int _abs(int n)
{

	if (n <= 0)
	{
		return (-n);
	}
	else
	{
		return (1);
	}
}
