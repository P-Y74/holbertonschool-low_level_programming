#include <stdio.h>
#include "main.h"

/**
 * mul - Multiplies two intergers
 * @a: fisrt number to multiply
 * @b: second number to multiply
 *
 * Return: mul
 */
int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
